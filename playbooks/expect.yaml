- name: expect
  hosts: localhost
  tasks:
  - name: expect 01
    ansible.builtin.expect:
      command: telnet {{ host }} {{ port }}
      responses:
        Enter\sChoice>:
          - "tacl\n"
        TACL\s1>:
          - "logon super.super\n"
          - "exit\n"
      timeout: 10
    register: salida

  - name: debug
    debug:
      var: salida
